<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <title>{{ upperFirst(page) }} - Dominote</title>
  {{ style('vendor/bootstrap/css/bootstrap.min.css') }}
  {{ style('vendor/font-awesome/css/font-awesome.min.css') }}
  {{ style('vendor/datatables/dataTables.bootstrap4.css') }}
  {{ style('css/sb-admin.css') }}
</head>
<body class="{{ bodyClass }}">
  @if(blank)
    <div class="container">
      @!section('content')
    </div>
  @else
    @!component('components.navbar', { page })

    <div class="content-wrapper">
      <div class="container-fluid">
        <ol class="breadcrumb">
          @!section('breadcrumb')
        </ol>

        @if(page === 'orders')
          @!section('content')
        @else
          <div class="row">
            <div class="col-12">
              @!section('content')
            </div>
          </div>
        @endif
      </div>

      <footer class="sticky-footer">
        <div class="container">
          <div class="text-center">
            <small>Copyright &copy; Dominote 2018</small>
          </div>
        </div>
      </footer>

      <a class="scroll-to-top rounded" href="#page-top">
        <i class="fa fa-angle-up"></i>
      </a>

      @include('includes.logout-modal')
    </div>
  @endif


  {{ script('vendor/jquery/jquery.min.js') }}
  {{ script('vendor/bootstrap/js/bootstrap.bundle.min.js') }}
  {{ script('vendor/jquery-easing/jquery.easing.min.js') }}
  {{ script('vendor/datatables/jquery.dataTables.js') }}
  {{ script('vendor/datatables/dataTables.bootstrap4.js') }}
  
  @loggedIn
    {{ script('js/sb-admin.min.js') }}
    {{ script('js/sb-admin-datatables.min.js') }}
  @endloggedIn

  @!section('scripts')
</body>
</html>